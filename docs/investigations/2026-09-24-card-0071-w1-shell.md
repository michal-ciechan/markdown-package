# CARD-0071 W1 shell verification

Date: 2026-09-24. Branch: `feat/card-task-fe17e22e`. Worktree:
`C:\Antiphon\worktrees\card-task-fe17e22e`. Original Code task: `fe17e22e`.
Plan: `docs/plans/2026-09-23-phase1-execution.md` section 3.1 and CARD-0071.
That plan has no `### Checkpoints` table or PC rows; no CP or PC IDs were
defined for W1. The generic `docs/testing-and-build.md` is absent in this
repository, whose W1 test lanes are Node, Playwright, Rust and manual Windows.

## Implementation

The Tauri v2 application is under `src/desktop/src-tauri/`. It serves the
viewer build from `src/web-viewer/dist`, uses the default Windows origin,
registers no plugins or commands, and bundles a per-user NSIS installer. The
spike's placeholder icons and inactive installer hook were copied; generated
schemas and Rust target output are ignored. No files under
`src/web-viewer/src/` changed.

Toolchain: Rust/Cargo 1.98.1 at `%USERPROFILE%\.cargo\bin`, Tauri CLI 2.11.5
installed with `cargo install tauri-cli --version "^2" --locked` (cold install
15m 22s), and `npm ci` in `src/web-viewer`. The `Cargo.lock` application
dependency graph resolves Tauri 2.11.5.

## Ordinary verification

| Check | Commit | Result | Evidence |
|---|---|---|---|
| Desktop guard tests, `node --test tests/*.test.mjs` from `src/desktop` | `bc8a3026650fc3ec8370ebd6901c4bfc67e78ff1` | 2 passed, 0 failed, 0 skipped | terminal output |
| Viewer Unit, `npm test` from `src/web-viewer` | `db7ed84f3e9942c2bbea914f9dcdaaa9974dfbf9` | 233 passed, 0 failed, 0 skipped | terminal output; viewer source unchanged afterward |
| Viewer build, `npm run build` | `db7ed84f3e9942c2bbea914f9dcdaaa9974dfbf9` | V-1 and V-2 passed; eager gzip 98,421 / 145,000 bytes | terminal output |
| `cargo tauri build --debug` from `src/desktop` | `38923026e65e559439b5952900eecc4fe90b6164` | passed in 264.4s; 12,556,800-byte exe and 2,501,873-byte NSIS installer | `C:\Antiphon\evidence\card-0071-tauri-build.log` |
| Native WebView2 smoke, `node tests/webview-smoke.mjs C:/Antiphon/evidence/card-0071-guide.mdpkg C:/Antiphon/evidence/card-0071-native` | `bc8a3026650fc3ec8370ebd6901c4bfc67e78ff1` | passed: 2 documents render; draft, recent and package reattach after process restart | `C:\Antiphon\evidence\card-0071-native\card-0071-native-smoke.json`, `card-0071-open.png`, `card-0071-reopened.png` |
| Full Playwright, `npm run test:browser` | `db7ed84f3e9942c2bbea914f9dcdaaa9974dfbf9` | 285 passed, 1 failed, 0 skipped (14.7m) | `C:\Antiphon\evidence\card-0071-playwright.log` |
| Full Playwright rerun, same command | `bc8a3026650fc3ec8370ebd6901c4bfc67e78ff1` | 286 passed, 0 failed, 0 skipped (9.9m) | `C:\Antiphon\evidence\card-0071-playwright-rerun.log` |

The first browser failure was Chromium `tests/username.spec.js:33`, a 5-second
wait for `.document-title` to become `guide.md`. This happened while the cold
Rust CLI install was compiling. The exact test passed at the pre-task base
commit `9ba5cf0` (1/1) and at this task's commit (1/1), then passed in the
full 286-case rerun. No timeout, assertion or retry was changed. Target logs:
`C:\Antiphon\evidence\card-0071-base-target.log` and
`C:\Antiphon\evidence\card-0071-current-target.log`.

The smoke package was made from `examples/guide-and-notes` with the documented
`dotnet run --project src/generator-cli/src/Mdpkg.Cli -- pack ...` command.
The `examples/` tree contains loose Markdown, so the generated `.mdpkg` is at
`C:\Antiphon\evidence\card-0071-guide.mdpkg`. This extra build supplied the
card's requested example content for native acceptance.

## Native acceptance boundary

In the real WebView2, `location.origin` was `http://tauri.localhost`,
`isSecureContext` was true, IndexedDB was available and
`showOpenFilePicker` existed. The enhanced **Open package** button was visible.
The smoke opened the package through the viewer's existing
`<input type=file>` route using CDP file injection, then saved and restored an
unfinished draft across process restart. The screenshots show the loaded
document and the restored session.

The enhanced `showOpenFilePicker` Windows dialog was invoked, but CDP did not
emit a file chooser event, so the harness could not select a file through that
dialog. Native Computer Use could not attach: its native pipe was unavailable
after a fresh session. A hands-on check of selecting the generated package in
that enhanced picker remains open. R1 (stable secure origin and persistence)
passed. R9's eager-graph budget remained below its 145,000-byte ceiling with
no viewer source change. R13's enhanced-picker selection remains unverified;
its paste and file-grant questions belong to W2. The picker selection is the
only ordinary acceptance item left for CARD-0071 before Review.

No W1 PC IDs or variants were defined. SourceLanding Mutation still owns any
future deliberate mutant and red/restore/green controls after land; the native
smoke and ordinary green runs do not substitute for that stage.

## Review smoke fixes (task e0b0d1d8)

Branch: `feat/card-task-e0b0d1d8`. Worktree:
`C:\Antiphon\worktrees\card-task-e0b0d1d8`. Original landing owner remains
Code task `fe17e22e`; this is a repair continuation from `cbae981`.

The native smoke now creates a temporary `WEBVIEW2_USER_DATA_FOLDER` per
invocation and reuses it only for that invocation's quit/relaunch check. It
waits for the WebView2 debug endpoint to close between processes, then waits
for the expected package name in `.recent-list` after IndexedDB restore. The
recent is in a collapsed `<details>`, so text presence, followed by the draft
value assertion, is the intended check. The temporary profile is removed after
shutdown. This smoke failed in review at the prior production line when it
read recents at DOM ready (`C:\Antiphon\evidence\card-0071-review-native-isolated\card-0071-native-smoke.json`);
that red result had a real missing-recent assertion. No timeout or assertion
was weakened. The restart lifecycle and collapsed-list locator each exposed
and received a separate fix before the two green runs.

| Check | Commit | Result | Evidence |
|---|---|---|---|
| Native build, `cargo tauri build --debug` | `2a7d52f` | Passed; debug exe and NSIS bundle built. V-1 passed, V-2 98,421/145,000 eager gzip bytes | `C:\Antiphon\evidence\card-0071-w1-smoke-fix-tauri-build.log` |
| Native WebView2 smoke, first and immediate second invocation | `bf56463` | Both passed: isolated profile, secure default origin, recent restored and unfinished draft restored after restart | `C:\Antiphon\evidence\card-0071-w1-fix-native-1\card-0071-native-smoke.json`, `C:\Antiphon\evidence\card-0071-w1-fix-native-2\card-0071-native-smoke.json`; adjacent screenshots and logs |
| Desktop guards, `node --test tests/*.test.mjs` | `bf56463` | 2 passed, 0 failed, 0 skipped | `C:\Antiphon\evidence\card-0071-w1-fix-guards.log` |
| Whole viewer Unit, `npm test` | `bf56463` | 233 passed, 0 failed, 0 skipped | `C:\Antiphon\evidence\card-0071-w1-fix-unit.log` |
| Full browser lane, `npm run test:browser` | `bf56463` | 286 passed, 0 failed, 0 skipped; V-1 and V-2 passed again | `C:\Antiphon\evidence\card-0071-w1-fix-playwright.log` |

`npm ci` in `src/web-viewer` was the unlisted setup command needed because
this fresh worktree had no `node_modules`. The W1 plan has no checkpoint table,
coverage-to-class list, or PC rows, so there are no CP-n/TRX results or PC
variants to report. R1 passed in both native restart runs. R9 passed at the
budget above. R13 still needs a human to select the package through WebView2's
enhanced `showOpenFilePicker`; the automated smoke used the existing standard
file-input route. Its paste and grant-persistence questions remain W2 work.
