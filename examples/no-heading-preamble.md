These are release notes for an internal tool. There is no heading anywhere in
this file, so under the source digest profile (`cm0312-source-lf-v1`, spec.md
section 6.1, rule 4) the whole document is the permanent preamble scope: a
document with no top-level heading is entirely preamble, and that preamble's
identity is permanent even when it later becomes empty or gains headings.

A later revision could add a first heading above this text without changing
the preamble's identity; only its scope would shrink to the text preceding
that new heading.
